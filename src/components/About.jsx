
export default function About() {    
    return (
        <div id="about" className="container mx-auto flex flex-col gap-4 px-4"> 
            <h2 className="text-4xl text-center">About</h2>
            <p className="text-base text-center">
                AtomicAssets is a Non-Fungible Token (NFT) standard for eosio blockchains. AtomicAsset NFTs are resource-efficient yet full-featured. All metadata is stored on-chain, and templates are used to efficiently store redundant data. Additional features include native trade offer functionality and on-chain notifications.
            </p>
        </div>

    );
}
